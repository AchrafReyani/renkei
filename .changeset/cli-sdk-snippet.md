---
"renkei": patch
---

`renkei add-client` (no preset, or `--preset public`) now also prints a `renkei-client` snippet with the issuer, client ID and redirect URI filled in, next to the raw OIDC client settings.
