{
  "name": "example-supabase-edge",
  "private": true,
  "description": "renkei as a Supabase Edge Function storing in the project's Postgres: the function is two lines importing renkei-server/supabase",
  "type": "module",
  "scripts": {
    "bundle:local": "node scripts/bundle-supabase.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
