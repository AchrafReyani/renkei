RENKEI_ISSUER=http://localhost:8787
RENKEI_CLIENT_ID=my-next-app
RENKEI_CLIENT_SECRET=my-next-app-secret-0123456789abcdef
# encrypts the session cookie — 32+ chars, e.g. `openssl rand -base64 32`
RENKEI_NEXT_SECRET=change-me-to-32-or-more-random-characters
